// Declare and initialize and enum
// basic begin with 0 and iota if no value is given
// 
// enum Color {Red,Green,Blue};
//
enum Color {Red = 5,Green,Blue};


// Declaration of an variable with type enum and assignment
let c: Color = Color.Green;

// To see the value of `c` in the terminal
console.log(c)
